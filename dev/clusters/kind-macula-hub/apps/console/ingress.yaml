apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: macula-console
  namespace: macula
  annotations:
    external-dns.alpha.kubernetes.io/hostname: console.macula.local
spec:
  ingressClassName: nginx
  rules:
  - host: console.macula.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: macula-console
            port:
              number: 80
