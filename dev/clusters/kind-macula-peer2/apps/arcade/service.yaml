apiVersion: v1
kind: Service
metadata:
  name: macula-arcade
  namespace: macula
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    targetPort: 4000
    protocol: TCP
  selector:
    app: macula-arcade
